export const setUserName = ({ commit }, type) => {
  commit('SET_USERNAME', type);
};

export const setUserId = ({ commit }, type) => {
  commit('SET_USERID', type);
};

export const setLoad = ({ commit }, type) => {
  commit('SET_LOAD', type);
};

